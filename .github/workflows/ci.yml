name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install package and dev dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'git-crawl @ git+https://github.com/alex-drocks/git-crawl.git@0f2eb881296e591a81e806c0689797c65cfdde77'
          python -m pip install -e '.[dev]'
      - name: Run tests
        run: python -m pytest tests -q
      - name: Run Ruff
        run: python -m ruff check tao_git_crawl tests
      - name: Compile package
        run: python -m compileall -q tao_git_crawl
      - name: Build distributions
        run: |
          rm -rf dist build tao_git_crawl.egg-info
          python -m build
      - name: Smoke install wheel and resolve fixture
        run: |
          tmpdir=$(mktemp -d)
          python -m venv "$tmpdir/.venv"
          "$tmpdir/.venv/bin/python" -m pip install --upgrade pip
          "$tmpdir/.venv/bin/python" -m pip install 'git-crawl @ git+https://github.com/alex-drocks/git-crawl.git@0f2eb881296e591a81e806c0689797c65cfdde77'
          "$tmpdir/.venv/bin/python" -m pip install dist/*.whl
          "$tmpdir/.venv/bin/tao-git-crawl" --help >/tmp/tao-git-crawl-help.txt
          "$tmpdir/.venv/bin/tao-git-crawl" crawl --help >/tmp/tao-git-crawl-crawl-help.txt
          "$tmpdir/.venv/bin/tao-git-crawl" resolve --from-json examples/subnets.sample.json --output-dir "$tmpdir/out" --target smoke
          "$tmpdir/.venv/bin/tao-git-crawl" resolve --from-json examples/subnets.sample.json --config examples/config.overrides.py --output-dir "$tmpdir/out-config" --target smoke
          test -s "$tmpdir/out/repository-manifest.json"
          test -s "$tmpdir/out-config/subnets/64/owner-targets.json"
