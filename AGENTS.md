# Agent Instructions

- For GitHub git operations in this repository, prefer SSH:
  `git@github.com:alex-drocks/tao-git-crawl.git`.
- Avoid HTTPS GitHub remotes for push/tag operations unless the user explicitly asks for HTTPS.
