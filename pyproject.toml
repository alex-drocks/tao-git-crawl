[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "tao-git-crawl"
version = "0.7.0"
description = "Resolve Bittensor subnet GitHub metadata and feed git-crawl."
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Alex Desroches" }]
dependencies = [
  "git-crawl>=0.3.0",
  "python-dotenv>=1.0,<2",
]

[project.optional-dependencies]
chain = ["substrate-interface>=1.7.11,<2"]
dev = ["pytest>=8.0", "ruff>=0.8", "build>=1.2"]

[project.scripts]
tao-git-crawl = "tao_git_crawl.cli:main"
tao-git-crawl-api = "tao_git_crawl.api:main"

[project.urls]
Homepage = "https://github.com/alex-drocks/tao-git-crawl"
Repository = "https://github.com/alex-drocks/tao-git-crawl"
Changelog = "https://github.com/alex-drocks/tao-git-crawl/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.sdist]
include = [
  "/.env.example",
  "/CHANGELOG.md",
  "/README.md",
  "/LICENSE",
  "/docs",
  "/registry",
  "/tests",
  "/tao_git_crawl",
]

[tool.hatch.build.targets.wheel]
packages = ["tao_git_crawl"]
force-include = { "registry/overrides.json" = "tao_git_crawl/registry_overrides.json" }

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
markers = [
  "docker_e2e: tests that build and run the Docker API service",
]
